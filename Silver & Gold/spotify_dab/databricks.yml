# This is a Databricks asset bundle definition for spotify_dab.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: spotify_dab
  uuid: e966bb58-7678-4352-a86b-b747aecf7bf8



targets:
  dev:
    mode: development
    default: true
    workspace:
      host: http://127.0.0.1:7073
    variables:
      catalog: databricksazureproject
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: http://127.0.0.1:7073
      # We explicitly deploy to /Workspace/Users/jayvkothawade@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/jayvkothawade@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricksazureproject
      schema: prod
    permissions:
      - user_name: jayvkothawade@gmail.com
        level: CAN_MANAGE
